<% if prev_page or next_page %>
  <nav>
    <ul class="pager">
      <% if prev_page %>
        <li class="previous">
          <%= link_to "&laquo; #{prev_page.header}", "/#{current_page_prefix}/#{prev_page.path}.html" %>
        </li>
      <% end %>
      <% if next_page %>
      <li class="next">
        <%= link_to "#{next_page.header} &raquo;", "/#{current_page_prefix}/#{next_page.path}.html" %>
      </li>
      <% end %>
    </ul>
  </nav>
<% end %>
